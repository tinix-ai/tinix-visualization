// chủ đề
export enum ThemeEnum {
  DARK = 'dark',
  LIGHT = 'light'
}

// ngôn ngữ
export enum LangEnum {
  EN = 'EN',
  VI = 'VI'
}

// hiển thị xem trước
export enum PreviewScaleEnum {
  FIT = 'fit',
  SCROLL_Y = 'scrollY',
  SCROLL_X = 'scrollX',
  FULL = 'full'
}